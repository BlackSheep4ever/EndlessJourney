string[] opçoes = {"Se examinar.", "Olhar em volta.", "Chamar por ajuda."};

            for (int i = 0; i < opçoes.Length; i++)
            {
                int rank = i + 1;
                Console.WriteLine(rank + "." + opçoes[i]);
            }

